import 'package:flutter/material.dart';

class CreativeUI extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Creative Interface')),
    );
  }
}
