import 'package:flutter/material.dart';

class ClientUI extends StatelessWidget {
  const ClientUI({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Client Interface')),
    );
  }
}
